import 'package:flutter/material.dart';

class NotesProfileScreen extends StatelessWidget {
  const NotesProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
